#include <stdio.h>

void main()
{
    float result, a, b ,c;
    scanf("%f", &a);
    scanf("%f", &b);
    scanf("%f", &c);
    if (c == 0) {
        printf("0으로 나눌 수 없습니다");
        return;
    }
    result = (a + b) * c - (a - b) / c;
    printf("%f", result);
}
