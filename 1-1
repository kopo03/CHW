#include <stdio.h>

void main()
{
    int result, a, b ,c;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    if (c == 0) {
        printf("0으로 나눌 수 없습니다");
        return;
    }
    result = (a + b) * c - (a - b) / c;
    printf("%d", result);
}
